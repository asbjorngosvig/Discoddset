services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: sommerhus
      POSTGRES_PASSWORD: sommerhus
      POSTGRES_DB: sommerhus
    ports:
      - "5432:5432"
    volumes:
      - sommerhus-db:/var/lib/postgresql/data

volumes:
  sommerhus-db:
